# Dependencies
node_modules/
 
# Environment / secrets — NEVER commit real .env
.env
.env.local
.env.*.local
 
# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
 
# Test coverage
coverage/

# Build output — the deploy zip is rebuilt from source by `npm run package`,
# so committing it would just add a binary that goes stale.
dist/
 
# OS / editor cruft
.DS_Store
Thumbs.db
